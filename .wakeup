#!/usr/bin/env bash

set -o errexit
set -o pipefail
set -o errtrace
set -u
shopt -s inherit_errexit 2>/dev/null || true

IFS=$'\n\t'

open -a Spotify

( date | tr -d '\n' && echo ' - wakeup' ) >> "${HOME}/.activity.log"
